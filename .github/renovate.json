{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    "schedule:daily",
    ":dependencyDashboard",
    ":semanticCommits",
    ":separateMajorReleases",
    ":rebaseStalePrs"
  ],
  "timezone": "Europe/Stockholm",
  "labels": ["dependencies", "renovate"],
  "rangeStrategy": "bump",
  "updateNotScheduled": false,
  "lockFileMaintenance": { "enabled": true, "schedule": ["after 10pm and before 10:30pm on wednesday"] },
  "vulnerabilityAlerts": { "enabled": true, "labels": ["security"] },
  "packageRules": [
    { "description": "Group all patch/minor updates into one PR per run (reduces CodeRabbit review-count load under the shared 5/hour quota); auto-merge the bundle on green CI + review", "matchUpdateTypes": ["minor", "patch"], "groupName": "non-major dependencies", "automerge": true },
    { "description": "Group GitHub Actions updates", "matchManagers": ["github-actions"], "groupName": "github-actions" },
    { "description": "Deliberately pinned to 2.86.2 in Package.swift to work around apple/swift-nio#3647 (Windows Sendable/IPPROTO compile errors) — Renovate auto-bumping this exact pin (PR #153) reintroduced the exact bug PR #149 fixed. Do not remove until upstream resolves #3647; see Package.swift comment.", "matchPackageNames": ["apple/swift-nio"], "enabled": false }
  ],
  "prHourlyLimit": 2,
  "prConcurrentLimit": 10,
  "schedule": [
    "after 10pm and before 10:30pm on wednesday"
  ]
}
